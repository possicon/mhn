info:
  name: Update a Module Copy 2
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/admin/module/:id"
  params:
    - name: id
      value: "9"
      type: path
  body:
    type: json
    data: |-
      {
          "name": "Reporting",
          "img": "url",
          "description": "This helps you to manage your hospital Emergency seamlessly."
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
