info:
  name: List Modules
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/admin/module"
  body:
    type: json
    data: |-
      {
          "name": "Reporting",
          "img": "url",
          "description": "This helps you to manage your hospital Emergency seamlessly."
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
