info:
  name: Delete a Module
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/admin/module/:id"
  params:
    - name: id
      value: "10"
      type: path
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
