info:
  name: Create Area
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/admin/health-area/create"
  headers:
    - name: Content-Type
      value: application/vnd.api+json
    - name: Accept
      value: application/vnd.api+json
  body:
    type: text
    data: |-
      {
          "name":"Ontology"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
