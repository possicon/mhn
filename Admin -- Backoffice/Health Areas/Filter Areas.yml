info:
  name: Filter Areas
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/admin/health-area/list"
  body:
    type: json
    data: "{\r\n    \"active\":false\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
