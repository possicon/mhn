info:
  name: Areas by Organization Type
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/admin/health-area/get"
  body:
    type: json
    data: "{\r

      \  \"organization_type_id\": 1\r

      }"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
