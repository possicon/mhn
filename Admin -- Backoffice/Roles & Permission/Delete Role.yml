info:
  name: Delete Role
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/admin/roles/:id"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/x-www-form-urlencoded
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: form-urlencoded
    data:
      - name: name
        value: Add Organizationz
      - name: guard_name
        value: user
      - name: group_name
        value: Owner
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
