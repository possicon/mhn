info:
  name: View Permission
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/admin/permissions/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
          "name":"Add Organization",
          "group_name":"Owner",
          "guard_name":"user"
      }
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
