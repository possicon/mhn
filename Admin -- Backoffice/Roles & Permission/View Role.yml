info:
  name: View Role
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}/admin/roles/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
          "name":"Add Organization",
          "group_name":"Owner",
          "guard_name":"user"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
