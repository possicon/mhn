info:
  name: Search licences
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/admin/licence/search?name=Driver"
  params:
    - name: name
      value: Driver
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
