info:
  name: download sample
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/admin/xrayTypes/download-sample"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
