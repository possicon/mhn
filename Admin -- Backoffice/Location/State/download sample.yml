info:
  name: download sample
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/admin/state/sample"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
