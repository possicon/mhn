info:
  name: sample download
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/admin/country/sample"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
