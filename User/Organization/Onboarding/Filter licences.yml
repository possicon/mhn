info:
  name: Filter licences
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/user/organization/onboarding/license"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
