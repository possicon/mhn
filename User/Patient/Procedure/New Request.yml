info:
  name: New Request
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/user/surgery/procedure/:procedureId"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: procedureId
      value: ""
      type: path
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
