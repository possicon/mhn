info:
  name: Overview
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/user/dashboard-count"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
