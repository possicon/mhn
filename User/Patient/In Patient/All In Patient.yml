info:
  name: All In Patient
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/user/all-in-patient"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
