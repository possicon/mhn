info:
  name: check paystack status
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/generic/payment-status/:reference"
  params:
    - name: reference
      value: jpoht2hwrw
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
