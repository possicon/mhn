info:
  name: Filter licences Copy
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/generic/organization/license"
  body:
    type: json
    data: |-
      {
          // "active":true,
          "country_id":"9bb76bef-d804-4b6c-9711-452aea125be9"  //country ID
          
          
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
