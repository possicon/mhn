info:
  name: List Single Health Area
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/generic/health-area/:id"
  params:
    - name: id
      value: 9b81fd3c-392c-4570-8f85-7122fc32308d
      type: path
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
