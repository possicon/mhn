info:
  name: List Modules
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/generic/organization-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
